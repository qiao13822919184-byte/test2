import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 1. 你的 GitHub Pages 主域名
  site: 'https://qiao13822919184-byte.github.io', 
  // 2. 你的仓库名字（必须以斜杠开头！）
  base: '/test', 
});